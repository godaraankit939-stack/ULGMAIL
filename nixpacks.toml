[phases.setup]
nixPkgs = ["playwright-driver", "python310", "glibc"]

[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "python3 -m playwright install chromium",
  "python3 -m playwright install-deps chromium"
]

[start]
cmd = "python3 bot.py"
